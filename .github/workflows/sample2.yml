name: Sub Workflow

on:
  workflow_run:
    workflows: ["Main Workflow"]
    types:
      - completed

jobs:
  sub_job:
    runs-on: ubuntu-latest

    steps:
      - name: Process Sub Workflow
        run: echo "Sub Workflow called with input: ${{ github.event.workflow_run.inputs.user_input }}"
